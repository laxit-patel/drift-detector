# Runtime dependencies for the Drift Detector plugin (the inventory-scan path).
# The bootstrap runner (bin/drift-scan) installs these into a plugin-local venv
# on first run. semgrep IS the scan engine (Opengrep-compatible), so no separate
# binary install is needed. The scanner's only third-party import is PyYAML.
PyYAML>=6.0
semgrep>=1.100
